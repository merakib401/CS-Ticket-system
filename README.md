১.  JSX হলো React এর একটি syntax যেখানে js. এর মধ্যে html  লেখা যায়। 

এটি কোড সহজে লেখা যায়। 
ui এবং logic এক জায়গায় লেখা যায়। কোড readable থাকে। 

২.  State হলো কম্পোনেন্টের নিজের ডাটা যা সময়ের সাথে চেঞ্জ হতে পারে এবং props হলো প্যারেন্ট কম্পোনেন্ট থেকে চাইল্ড কম্পোনেন্টে ডাটা পাঠানোর উপায় 

৩.  useState hook হলো ফাংশন কম্পোনেন্টের ভিতরে ডাটা তৈরি বা আপডেট করার মাধ্যম।
এটি  দুইটা প্যারামিটার নেয় একটি  বর্তমান value ধরে রাখে অন্যটি value change করে। 

৪.  lifting state up এর মাধ্যমে এক কম্পোনেন্টের state অন্য কম্পোনেন্টে শেয়ার করা হয় 

৫.   React এ কয়েক ভাবে event handle করা যায়  যেমন সাধারণ function দিয়ে, Inline event handling, event object, 